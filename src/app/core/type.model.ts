export interface Title {
    name: string,
    class: string
}

export interface Table {
    first?: string,
    last?: string,
    full?: string,
    phone?: string,
    email?: string,
    birthday?: string
}