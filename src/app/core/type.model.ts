export interface Title {
    name: string,
    class: string
}

export interface Item {
    first?: string,
    last?: string,
    full?: string,
    phone: string,
    email: string,
    address?: string,
    birthday?: string,
    detail?: boolean
}
